# TODO.md

1. **Editing experience** — show a diff before applying edits and allow undo.
2. **UI improvement** — syntax highlighting, colors, and a spinner while waiting.
3. **Session history** — save and reload past conversations.
4. **External tools** — let the agent run user-defined scripts or call web APIs.
5. **Skills** — reusable prompt templates for common tasks like code review.
6. **Configuration file** — ~~a simple config file for API keys, model, and preferences.~~ ✓ done: `~/.ma/settings.json` with hot-reload
7. **Auto-approve mode** — a `-y` flag to skip confirmation prompts.
8. **Context awareness** — auto-detect project type and respect `.gitignore`.
